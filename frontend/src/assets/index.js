// Only the assets the app actually uses. The old UniComm marketing/icon set was
// removed in the 2Gather redesign — most chat icons are now inline SVGs in
// components/ui/icons.jsx, and the brand mark is an SVG favicon + CSS gradient.
import showPass from "./showPassword.jpeg";
import hidePass from "./hidePassword.jpeg";
import searchIcon from "./searchIcon.svg";
import plusIcon from "./plusIcon.svg";
import googleLogo from "./googleLogo.svg";

export { showPass, hidePass, searchIcon, plusIcon, googleLogo };
